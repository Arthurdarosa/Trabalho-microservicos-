{
  "name": "smart-lockers-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd svc-lockers && npm install && cd ../svc-condominos && npm install && cd ../svc-entregas && npm install && cd ../svc-logging && npm install && cd ../svc-controle-abertura && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
